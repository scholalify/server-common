{
  "name": "@dliya/scholalify-common",
  "version": "1.0.1",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "clean": "rm -r ./build/*",
    "build": "npm run clean && npx tsc",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish"
  },
  "keywords": [],
  "author": "Dilum Liyanage",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
